# 🚀 Quick Start - Super Bowl Analytics

## 5-Minute Setup

### 1️⃣ Get API Keys (5 min)

**GEMINI_API_KEY:**
- Go to https://ai.google.dev
- Click "Get API Key"
- Copy your key

**VEO_API_KEY:** ⭐ IMPORTANT FOR VIDEO
- Go to https://fal.ai
- Sign up / Login
- Go to Settings → API Keys
- Copy your `key:secret` (both parts!)

### 2️⃣ Configure Backend (2 min)

```bash
cd backend
cp .env.example .env
```

Edit `backend/.env`:
```
GEMINI_API_KEY=your_gemini_key_here
VEO_API_KEY=your_fal_key:your_fal_secret_here
```

### 3️⃣ Run Backend (1 min)

```bash
cd backend
python3 -m uvicorn main:app --reload
```

### 4️⃣ Run Frontend (1 min)

```bash
cd client
npm run dev
```

## ✅ Test It

1. Open http://localhost:5173
2. Start live camera capture
3. Click "Capture" button → should see spinner
4. See lightning bolt ✅
5. Check highlights section for image
6. Repeat 4 times for halftime video

## 🎯 What Happens

| Action | What Happens |
|--------|-------------|
| **Click Capture** | Frame extracted from video |
| **Spinner appears** | Sent to backend for analysis |
| **Lightning bolt** | Analysis complete, highlight created |
| **Image appears** | AI image generating... |
| **Image updates** | Gemini Image API completed |
| **4+ captures** | Halftime video generation starts |
| **30-120 seconds** | Video generated by Veo 3.1 |
| **Play button** | Click to view video |

## 🔍 Check Setup

```bash
# Backend health
curl http://localhost:8000/health

# Video generation status
curl http://localhost:8000/video_generation_status
```

Both should show `enabled: true`

## 🐛 Issues?

**Capture button disabled?**
- Start live stream first (camera or screen)

**No highlight appearing?**
- Check browser console (F12) for errors
- Look for "captureHighlight called" logs

**AI image not generating?**
- Check GEMINI_API_KEY is correct
- Verify Gemini API enabled in Google Cloud

**Halftime video not appearing?**
- Capture at least 4 frames first
- Wait 1-2 minutes for generation
- Check console for "Generating halftime video" log

## 📚 Need More Help?

- **Full Setup**: See `SETUP_GUIDE.md`
- **Frame Capture**: See `CAPTURE_FEATURE.md`
- **Halftime Video**: See `VEO_INTEGRATION.md`
- **All Features**: See `FEATURE_SUMMARY_JAN31.md`

---

**That's it! You're ready to go!** 🎉
